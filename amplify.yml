version: 1
frontend:
  phases:
    build:
      commands:
        - npm ci
        - npm test
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
